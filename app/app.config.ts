export default defineAppConfig({
  ui: {
    colors: {
      primary: 'amber',
      neutral: 'stone'
    },
    formField: {
      slots: {
        description: 'text-xs text-dimmed'
      }
    }
  }
})

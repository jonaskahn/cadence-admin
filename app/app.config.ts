export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'stone'
    },
    formField: {
      slots: {
        description: 'text-xs text-dimmed'
      }
    }
  }
})
